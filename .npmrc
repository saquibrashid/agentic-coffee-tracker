# Microsoft IT policy: the public npm registry is not an allowed source. All
# package resolution goes through the approved Microsoft feed proxy, which
# mirrors npm. Lockfile entries are registry-independent (name@version plus an
# integrity hash), so switching the registry does not invalidate them.
registry=https://packagefeedproxy.microsoft.io/npm/
